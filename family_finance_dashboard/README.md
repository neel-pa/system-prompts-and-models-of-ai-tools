# Family Finance Dashboard

This is a simple command-line tool for tracking assets, liabilities, and income for multiple family members. It stores data in a JSON file and summarizes each person's net worth.

## Usage

Run `python dashboard.py` and follow the prompts to add family members and record their finances. A `data.json` file will store the information so it can be loaded next time.
